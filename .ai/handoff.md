# Handoff

State of the project as of 2026-04-25. Read this first when resuming after a context break.

## Repo state

- **Repo:** https://github.com/sagarbpatel31/tracemind (public)
- **Current branch:** `add-ai-engineering-system`
- **Base branch:** `main` (7 commits)
- **Frontend:** https://tracemind.vercel.app (deployed but non-functional — points to localhost)

## What is complete

| Area | Status | Notes |
|------|--------|-------|
| FastAPI backend | ✅ Complete | Auth, devices, incidents, ingest, seed, replay bundles |
| Next.js frontend | ✅ Complete | Dashboard, incident detail, login, API client |
| JWT auth | ✅ Complete | Register, login, `require_current_user` dependency |
| Go edge agent | ✅ Complete | Registers device, sends metrics/logs; collector stubs need real `/proc` reads |
| Python ROS2 collector | ✅ Complete | Simulation mode works; needs live ROS2 env to run real |
| Rules-based analysis | ✅ Complete | 7 rules, confidence scores, fallback |
| Claude Haiku LLM summary | ✅ Complete | `generate_llm_summary()` — disabled without `ANTHROPIC_API_KEY` |
| Replay bundles | ✅ Complete | zip with metadata, events, metrics, deployment, analysis_summary |
| Docker Compose local dev | ✅ Complete | postgres + api + web |
| Production Dockerfile | ✅ Complete | Respects `$PORT`, production-ready |
| render.yaml | ✅ Complete | Render IaC — auto-detects on Blueprint import |
| Vercel deploy | ✅ Complete | Frontend live |
| GitHub repo public | ✅ Complete | LICENSE (source-available), SECURITY.md, secret scanning on |
| caveman | ✅ Installed | `/caveman`, `/caveman-commit`, `/caveman-review` available |
| graphify | ✅ Installed | 263 nodes, 479 edges; git hooks rebuild on commit/branch |
| claude-mem | ✅ Installed | Worker on 127.0.0.1:37701; auto-injects session context |

## What is NOT done (blockers for live production)

1. **Supabase DB not provisioned** — user needs to sign up, create project, copy URI
2. **Render API not deployed** — user needs to sign up, Blueprint import, set DATABASE_URL
3. **Vercel `NEXT_PUBLIC_API_URL` not set** — still pointing to localhost; needs Render URL
4. **Production DB not seeded** — needs `POST /api/v1/seed/demo` after Render deploy

## Demo credentials (for seeded DB)

```
email:    demo@tracemind.ai
password: demo123
```

## Key env vars needed for production

| Var | Where | Value |
|-----|-------|-------|
| `DATABASE_URL` | Render | Supabase URI (`postgresql+asyncpg://...` or `postgres://...` — both work) |
| `ANTHROPIC_API_KEY` | Render | From console.anthropic.com (optional — enables LLM summaries) |
| `JWT_SECRET_KEY` | Render | Auto-generated by Render (`generateValue: true` in render.yaml) |
| `NEXT_PUBLIC_API_URL` | Vercel | `https://tracemind-api.onrender.com` |

## Local dev quick start

```bash
cd /Users/sagarpatel/Documents/Tracemind/deploy/docker-compose
docker compose up -d
curl -X POST http://localhost:8000/api/v1/seed/demo
open http://localhost:3000
# login: demo@tracemind.ai / demo123
```

## Tool paths (macOS arm64)

```
uv:         /Users/sagarpatel/.local/bin/uv
graphify:   /Users/sagarpatel/.local/bin/uv tool run --from graphifyy graphify
bun:        /Users/sagarpatel/.bun/bin/bun
docker:     /Applications/Docker.app/Contents/Resources/bin/docker
```

## Current branch intent

`add-ai-engineering-system` — adding `.ai/` context layer + `CLAUDE.md` + `AGENTS.md`. No production code changes. Merge to main when files are complete.
