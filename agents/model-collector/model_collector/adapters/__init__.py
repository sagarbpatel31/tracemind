"""Framework adapters for the Watchpoint model-collector.

Available:
  pytorch_adapter  — register_forward_hook based capture (Week 1)
  onnx_adapter     — ONNX Runtime profiler capture (future)
  tensorrt_adapter — TensorRT IExecutionContext capture (future)
"""
