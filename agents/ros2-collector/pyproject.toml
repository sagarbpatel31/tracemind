[project]
name = "tracemind-ros2-collector"
version = "0.1.0"
description = "TraceMind ROS2 collector for topic and node monitoring"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27",
    "pydantic>=2.0",
]
